<li class="c-accordion-nav__item  js-accordion-nav-item">
  {% include '_components/accordion-head' with { heading: 'Releases' } %}

  <div class="c-accordion-nav__content  js-accordion-nav-content">
    <div class="c-carousel  js-carousel">
      {% for entry in craft.entries.section('releases') %}
      <div class="c-carousel__slide  js-carousel-slide"{% if entry.backdropImage | length %} style="background-image: url({{ entry.backdropImage.first().getUrl() }});"{% endif %}>
        <div class="o-layout  o-layout--middle  o-layout--flush  o-layout--reverse  c-accordion-nav__layout">
          <div class="o-layout__item  u-1/2@desktop  c-carousel__item">
            {% if entry.media | length %}
              <div class="c-carousel__asset  js-carousel-asset">
                {% for block in entry.media %}
                  {% if block.type == "image" %}
                    <img src="{{ block.asset.first().getUrl() }}" />
                  {% elseif block.type == "youtube" %}
                    <div class="c-video">
                      <iframe src="{{ block.linkUrl }}" width="100%" frameborder="0" allowfullscreen></iframe>
                    </div>
                  {% endif %}
                {% endfor %}
              </div>
            {% endif %}
          </div>
          <div class="o-layout__item  c-accordion-nav__layout-item  c-carousel__item  u-1/2@desktop  js-carousel-right-item">
            {% if entry.body | length %}
            <div class="c-accordion-nav__details  js-accordion-nav-details">
              <div class="c-carousel__controller  u-flex  u-flex--space-between">
                <div class="c-carousel__pagination">{{ loop.index }} / {{ loop.length }}</div>
                <div class="u-flex  u-flex--space-between">
                  <div class="c-carousel__prev  js-carousel-prev">Prev</div>
                  <span class="c-carousel__slash">&frasl;</span>
                  <div class="c-carousel__next  js-carousel-next">Next</div>
                </div>
              </div>
              <h1 class="o-heading  o-heading--bold">{{ entry.title }}</h1>
              <div class="o-copy">
                <span>{{ entry.label }}</span> &frasl; <span>{{ entry.releasedYear }}</span>
              </div>
              <div class="o-copy">{{ entry.body }}</div>

              <!-- <p>
                <iframe width="100%" style="max-width: 320px;" height="20" scrolling="no" frameborder="no" src="https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/tracks/125995938&amp;color=ff5500&amp;auto_play=false&amp;hide_related=false&amp;show_comments=false&amp;show_user=false&amp;show_reposts=false"></iframe>
                <iframe width="100%" height="166" scrolling="no" frameborder="no" src="https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/tracks/125995938&amp;color=ff5500&amp;auto_play=false&amp;hide_related=false&amp;show_comments=true&amp;show_user=true&amp;show_reposts=false"></iframe>
                <iframe style="border: 0; width: 22px; height: 22px;" src="https://bandcamp.com/EmbeddedPlayer/album=1870857237/size=small/bgcol=000000/linkcol=ffffff/artwork=none/transparent=true/"></iframe>
                <iframe width="80%" height="60" style="max-width: 320px;" src="https://www.mixcloud.com/widget/iframe/?feed=https%3A%2F%2Fwww.mixcloud.com%2FNTSRadio%2Fspace-afrika-nts-manchester-9th-october-2016%2F&hide_cover=1&mini=1&hide_artwork=1" frameborder="0"></iframe>
              </p> -->

              <div class="c-soundcloud-player">
                <div class="c-soundcloud-player__play-button">
                  <i class="bts  bt-play-circle"></i>
                </div>
              </div>


              {% if entry.links | length %}
                {% for block in entry.links %}
                <div class="o-permalink">
                  <a href="{{ block.linkUrl }}" target="_blank">{{ block.linkText }}</a>
                </div>
                {% endfor %}
              {% endif %}
            </div>
            {% endif %}
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</li>
